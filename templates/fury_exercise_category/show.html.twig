{% extends 'base.html.twig' %}

{% block title %}FuryExerciseCategory{% endblock %}

{% block body %}
    <h1>FuryExerciseCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fury_exercise_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ fury_exercise_category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ fury_exercise_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fury_exercise_category_index') }}">back to list</a>

    <a href="{{ path('fury_exercise_category_edit', {'id': fury_exercise_category.id}) }}">edit</a>

    {{ include('fury_exercise_category/_delete_form.html.twig') }}
{% endblock %}
