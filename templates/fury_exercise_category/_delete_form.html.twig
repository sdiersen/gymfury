<form method="post" action="{{ path('fury_exercise_category_delete', {'id': fury_exercise_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fury_exercise_category.id) }}">
    <button class="btn">Delete</button>
</form>
