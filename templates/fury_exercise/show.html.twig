{% extends 'base.html.twig' %}

{% block title %}FuryExercise{% endblock %}

{% block body %}
    <h1>FuryExercise</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fury_exercise.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ fury_exercise.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ fury_exercise.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fury_exercise_index') }}">back to list</a>

    <a href="{{ path('fury_exercise_edit', {'id': fury_exercise.id}) }}">edit</a>

    {{ include('fury_exercise/_delete_form.html.twig') }}
{% endblock %}
